@model TerrariaDB.ViewModels.Terraria.CurrencyType.CurrencyTypeIndexViewModel

@{
    ViewData["Title"] = "Currency Types";
}

<div class="container mt-4">
    <div class="card shadow">
        <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
            <h4 class="mb-0">Currency Types</h4>
            <div>
                <a asp-action="Index" asp-controller="Home" class="btn btn-outline-light btn-sm me-2">
                    <i class="bi bi-house"></i> Home
                </a>
                @if (User.IsInRole("Admin"))
                {
                    <a asp-action="Create" class="btn btn-success btn-sm">
                        <i class="bi bi-plus-circle"></i> Create New
                    </a>
                }
            </div>
        </div>
        <div class="card-body">
            @if (!Model.CurrencyNames.Any())
            {
                <div class="alert alert-info">
                    <i class="bi bi-info-circle"></i>
                    No currency types found.
                </div>
            }
            else
            {
                <div style="max-height: 500px; overflow-y: auto;">
                    <table class="table table-hover">
                        <thead class="sticky-top bg-white">
                            <tr>
                                <th>Currency Name</th>
                                @if (User.IsInRole("Admin"))
                                {
                                    <th class="text-end">Actions</th>
                                }
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var currencyName in Model.CurrencyNames)
                            {
                                <tr>
                                    <td class="align-middle">
                                        <span class="fw-bold">@currencyName</span>
                                    </td>
                                    @if (User.IsInRole("Admin"))
                                    {
                                        <td class="text-end">
                                            <div class="btn-group btn-group-sm">
                                                <a asp-action="Edit" asp-route-id="@currencyName" class="btn btn-warning">
                                                    <i class="bi bi-pencil"></i> Edit
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@currencyName" class="btn btn-danger">
                                                    <i class="bi bi-trash"></i> Delete
                                                </a>
                                            </div>
                                        </td>
                                    }
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>
