@model TerrariaDB.ViewModels.Terraria.Item.ItemDeleteViewModel

@{
    ViewData["Title"] = "Delete Item";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow border-danger">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-exclamation-triangle"></i> Delete Item
                    </h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <i class="bi bi-exclamation-triangle"></i>
                        Are you sure you want to delete this item?
                    </div>

                    <div class="text-center mb-4">
                        <div class="d-inline-block p-3 border rounded bg-light">
                            <img src="@Model.Sprite" alt="@Model.Name" class="img-fluid" style="max-width: 100px;" />
                            <div class="mt-2">
                                <strong class="fs-5">@Model.Name</strong>
                            </div>
                        </div>
                    </div>

                    @if (Model.HasRelatedRecipes)
                    {
                        <div class="alert alert-warning">
                            <i class="bi bi-exclamation-circle"></i>
                            <strong>Warning:</strong> All related recipes will be deleted!
                        </div>
                    }

                    @if (Model.IsLastCraftingStationItem)
                    {
                        <div class="alert alert-danger">
                            <i class="bi bi-x-circle"></i>
                            <strong>Cannot delete:</strong> This item is the last item related to a crafting station!
                        </div>
                    }

                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="ItemId" />
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-danger" disabled="@Model.IsLastCraftingStationItem">
                                <i class="bi bi-trash"></i> Confirm Delete
                            </button>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Back
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
