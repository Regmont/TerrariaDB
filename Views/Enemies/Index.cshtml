@model IEnumerable<TerrariaDB.Models.Terraria.Enemy>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HostileEntity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HostileEntity.HostileEntityId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EnemyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EnemyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EnemyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
