@model TerrariaDB.Models.Terraria.Boss

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Boss</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="BossName" class="control-label"></label>
                <input asp-for="BossName" class="form-control" />
                <span asp-validation-for="BossName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SummonItemId" class="control-label"></label>
                <select asp-for="SummonItemId" class ="form-control" asp-items="ViewBag.SummonItemId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
